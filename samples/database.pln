// PlainScript 1.0.2-latest: SQLite query blocks.
database "sample.db" using "wasm"
execute
    CREATE TABLE IF NOT EXISTS notes (body TEXT)
done
remember notes as query
    SELECT body FROM notes
done