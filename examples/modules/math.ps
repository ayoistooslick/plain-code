// PlainScript v1.0.0-latest — reusable module, imported by app.ps

remember PI as 3.14

make double(n)
    give n * 2
done

make square(n)
    give n * n
done

make circumference(radius)
    give 2 * PI * radius
done

export PI
export double
export square
export circumference