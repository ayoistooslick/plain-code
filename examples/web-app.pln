// PLINJS v0.6 — Web App (Express shorthand)

web app

route "/"
    reply "Hello from PLINJS!"
done

route "/about"
    reply "About PLINJS"
done

route "/api/status"
    reply json
        status is "ok"
        version is "0.6"
    done
done

start 3000
