// PlainScript 1.0.35: WhatsApp QR login.

whatsapp bot
    auth "plain-script-whatsapp"
    login qr
    on message
        log message
        if message.text is "/start"
            reply "Welcome"
        done
    done
done