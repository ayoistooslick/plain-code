Review and finish the multi-rule Complex Compilation implementation before v2.0.0-latest.

The goal is to make multi-rule compilation work consistently in BOTH local and hosted/remote AI compilation paths.

1. Fix the hosted compiler contract so /translate can receive and use ALL matched rule definitions/rule paths instead of only one rulePath. The hosted compiler must support the same rule-composition architecture as the local provider path.

2. Preserve backward compatibility with requests that provide a single rulePath.

3. Ensure multi-rule compilation correctly combines constructs from unrelated rules in one Plain source. Test combinations such as:
   - REST API + HTTP fetch
   - REST API + cron
   - cron + email
   - REST API + fetch + cron + email
   - WebSocket + other applicable constructs where valid

4. Ensure the generated JavaScript preserves EVERY applicable construct. A rule being lower-scoring must never cause its constructs to disappear.

5. Ensure dependencies are the union of:
   - every matched rule's metadata dependencies
   - AI-generated dependencies
   Deduplicate the final dependency list.

6. Ensure imports, async status, validation, caching, and rule metadata remain correct when multiple rules are involved.

7. Fix the cron rule metadata mismatch:
   cron.md currently documents:
       every 5 minutes as
           ...
       done
   but cron.json does not have a trigger that reliably resolves "every <n> minutes".
   Add appropriate resolver coverage for the documented `every <n> minutes` syntax.
   Do not make the resolver accept arbitrary English scheduling requests. It must remain strict and rule-based.

8. Review every existing rule's .md and .json pair for the same class of mismatch:
   - syntax documented but not resolvable
   - syntax resolvable but undocumented
   - trigger patterns too broad
   - trigger patterns too narrow
   - dependencies missing from metadata
   - incorrect async metadata
   - examples that do not correspond to actual resolver syntax

9. Add regression tests for:
   - cron + email composition
   - REST API + fetch composition
   - REST API + cron composition
   - multiple rules with overlapping dependencies
   - remote multi-rule translation
   - `every <n> minutes`
   - rejection of arbitrary natural-language scheduling requests

10. Most importantly, preserve Plain's strict IOPL design:
    Complex Compilation is NOT an English-language compiler.
    AI may compose and compile constructs only when those constructs are covered by explicit Plain rules.
    Never solve missing syntax by allowing arbitrary English instructions.
    If no rule covers a construct, compilation must fail with a useful error explaining that a matching Plain rule is required.

11. Verify the final generated JavaScript with the existing validator and ensure validation occurs against the actual execution context, including async wrapping where required.

12. Run:
    node tests/ai.test.js
    node tests/telegram.test.js
    node tests/compiler.test.js

    Clearly distinguish new failures from known pre-existing environment/network failures.

13. Do not weaken security checks, dependency allowlists, require restrictions, secret handling, or provider isolation.

14. Do not modify package-lock.json unless genuinely required by an intentional dependency change.

15. Update relevant documentation and examples if the public compiler contract changes.

Add support for backtick strings in Plain.

Features:

1. Single-line backtick strings:
   remember name as `Ayo`

2. Multiline strings:
   remember html as `
   <h1>Hello</h1>
   `

3. Interpolation:
   `Hello ${user}`
   `${a + b}`

4. Support in:
   - show
   - reply
   - fetch bodies
   - email text/html
   - SQL queries
   - JSON generation
   - HTML generation

5. Parser:
   Backtick strings must preserve whitespace and line breaks.

6. Validator:
   Ensure generated JavaScript template literals are syntactically valid.

7. Security:
   Interpolation must compile to JavaScript expressions only.
   Do not evaluate interpolation during compilation.

8. Tests:
   - single line
   - multiline
   - interpolation
   - nested expressions
   - email templates
   - fetch JSON body templates

9. Documentation:
   Add a String Templates section to docs and README.

10. Maintain strict IOPL principles:
    Backticks are string syntax, not arbitrary code execution.
    
When finished, report:
- files changed
- architectural changes
- new tests
- exact test results
- any remaining limitations
- whether hosted and local multi-rule compilation now use equivalent rule-composition behavior