name: Build Acode Plugin

on:
  push:
    branches:
      - main
    paths:
      - "plinjs-acode/**"
      - ".github/workflows/build.yml"

  pull_request:
    paths:
      - "plinjs-acode/**"
      - ".github/workflows/build.yml"

  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Build Acode Plugin
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run PLINJS tests
        run: npm test

      - name: Verify Acode plugin files
        run: |
          test -f plinjs-acode/plugin.json
          test -f plinjs-acode/main.js

          echo "Acode plugin files:"
          ls -lah plinjs-acode

      - name: Package Acode plugin
        run: |
          mkdir -p dist

          cd plinjs-acode
          zip -r ../dist/plinjs-acode-plugin.zip . \
            -x "*.git*" \
            -x "node_modules/*"

      - name: Verify plugin ZIP
        run: |
          echo "Plugin package:"
          ls -lh dist/plinjs-acode-plugin.zip

          echo
          echo "ZIP contents:"
          unzip -l dist/plinjs-acode-plugin.zip

      - name: Upload Acode plugin
        uses: actions/upload-artifact@v4
        with:
          name: plinjs-acode-plugin
          path: dist/plinjs-acode-plugin.zip
          if-no-files-found: error