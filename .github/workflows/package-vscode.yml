name: Package PlainScript VS Code Extension

on:
  push:
    paths:
      - "plainscript-vscode/**"
      - ".github/workflows/package-vscode.yml"

  workflow_dispatch:

jobs:
  package:
    name: Build VSIX
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: plainscript-vscode

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22

      - name: Install dependencies
        run: npm install

      - name: Package PlainScript VS Code extension
        run: npx @vscode/vsce package

      - name: Upload VSIX
        uses: actions/upload-artifact@v4
        with:
          name: plainscript-vscode-extension
          path: plainscript-vscode/*.vsix
          if-no-files-found: error
          retention-days: 30