# ID verification template

This starter demonstrates a small HTTP verification service. Run it from the
repository root with `node compiler/cli.js check templates/idverify/src/app.pln`.

## Build

```bash
npx plainscript build src/app.pln -o build/app.js
node build/app.js
```