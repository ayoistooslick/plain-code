{
  "name": "@plainscript/idverify-sdk",
  "version": "1.0.02",
  "description": "An ID-verification npm SDK authored 100% in PlainScript. Fingerprints documents with SHA-256, parses machine-readable zones, and verifies document records.",
  "main": "dist/index.js",
  "type": "commonjs",
  "scripts": {
    "build": "plainscript build",
    "prepare": "plainscript build",
    "demo": "plainscript run src/demo.pln"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "plainscript-lang": "^1.0.02"
  }
}
