// idverify · src/demo.pln
// A runnable demo that uses the SDK entry point to verify a document.

import "./index.pln"

remember doc as
    holder is "ADA LOVELACE"
    documentType is "P"
    number is "UTOLOVELACE"
done

remember result as verifyDocument(doc)
show jsonEncode(result)
