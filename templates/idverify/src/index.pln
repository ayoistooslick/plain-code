// idverify · src/index.pln
// The public entry point for the SDK. Imports every capability module and
// re-exports its public API so `require("idverify")` returns the functions.

import "./hash.pln"
import "./mrz.pln"
import "./verify.pln"

export fingerprint
export digestText
export parseMrz
export verifyDocument
