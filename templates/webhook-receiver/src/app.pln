// PlainScript 1.0.35: authenticated webhook receiver.

web app
require api key from env("WEBHOOK_KEY")

route post "/webhooks/events"
    remember event as body()
    writeFile("last-webhook.json", jsonEncode(event))
    show "Webhook accepted"
    status 202
    reply json
        accepted is true
    done
done

start 3000