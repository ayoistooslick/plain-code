# Document OCR API

An MIT-licensed upload endpoint that extracts text from a document image.
PlainScript handles upload limits, MIME allow-lists, and OCR cleanup.

```bash
npx plainscript build src/app.pln -o dist/app.js
node dist/app.js
```