# REST API service

An MIT-licensed JSON API starter with health checks, request validation, and a
small in-memory collection. Replace the collection with a database when the
service needs durable storage.

```bash
npx plainscript build src/app.pln -o dist/app.js
node dist/app.js
```