# OAuth template

This starter keeps provider-specific credentials in environment variables and
returns a redirect from a PlainScript route. Add the provider SDK with `use`
when the application needs one.