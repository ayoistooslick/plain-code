// PlainScript 1.0.2-rfc: OAuth route starter.
web app
route get "/auth/start"
    remember provider as query("provider")
    redirect to `https://example.com/oauth/authorize?provider=${provider}`
done
route get "/auth/callback"
    remember code as query("code")
    reply json
        received is code
    done
done
start 3000