Implement the finalized WhatsApp Bot MVP in Plain v2.1.1 using the deterministic compiler only.

FINAL USER SYNTAX:

whatsapp bot
    auth "session"
    login pairing "2348012345678"

    on message
        log message

        if message.text is "/start"
            reply "Welcome!"
        done

        if message.text is "/help"
            reply "Available commands: /start /help"
        done
    done
done

Also support:

whatsapp bot
    auth "session"
    login qr

    on message
        log message
    done
done

Implementation requirements:
- Use @whiskeysockets/baileys.
- `whatsapp bot` must deterministically compile to the Baileys runtime.
- Internally create the Baileys socket with `makeWASocket()`.
- Implement auth/session persistence.
- Implement QR login.
- Implement pairing-code login.
- Pairing phone numbers must be validated according to Baileys requirements.
- Handle connection lifecycle/reconnection appropriately.
- Persist credential updates.
- Handle `messages.upsert`.
- Process only appropriate new-message notifications and prevent the bot from responding to its own messages.
- Normalize the incoming message into Plain's `message` object.
- Support `log message`.
- Support `message.text`.
- Support `reply "..."` inside `on message`, replying to the current WhatsApp chat.
- Support the `/start` and `/help` conditional examples exactly.
- Keep the Baileys API completely hidden from Plain users.

IMPORTANT:
Do not expose `makeWASocket()`, `sock.ev`, `messages.upsert`, `remoteJid`, or Baileys auth APIs in Plain syntax. They are runtime implementation details.

Also implement proper multiline backtick strings in the GENERAL Plain lexer/parser:

reply `heyy
how you dey?`

must preserve the newline.

Backtick strings must support normal multiline content without being a WhatsApp-specific feature. Preserve existing quoted-string behavior and support normal escapes such as \n, \t, \\, and escaped quotes where applicable.

Do not invent alternative WhatsApp syntax.

Test everything yourself using shell commands:
1. compiler/check tests
2. generated JavaScript
3. runtime startup
4. pairing-code flow as far as the environment permits
5. QR flow as far as the environment permits
6. simulated/mock message event
7. `log message`
8. `message.text`
9. `/start` reply
10. `/help` reply
11. multiline backtick strings
12. regression suite

Add deterministic compiler and runtime integration tests.

Do not use Complex Compilation. It no longer exists.

Do not commit or push.